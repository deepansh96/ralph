# Test Prompt for Ralph

Do the following steps:

1. Read the file ralph.sh in this directory
2. Run `ls -la` to list files
3. Run `date` to show the current time
4. Explain in 3 bullet points what ralph.sh does
5. Say "All done!" at the end
